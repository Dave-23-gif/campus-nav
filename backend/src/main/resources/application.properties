# ============================================================
#  Campus Navigation System — Spring Boot Configuration
# ============================================================

spring.application.name=campus-nav

# --- Database ---
spring.datasource.url=jdbc:mysql://localhost:3306/campus_nav?useSSL=false&serverTimezone=UTC
spring.datasource.username=campus_user
spring.datasource.password=campus_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- SQL Initialization ---
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# --- Server ---
server.port=8080

# --- CORS (allow frontend during dev) ---
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500

# --- JWT Secret (change in production) ---
jwt.secret=CampusNavBaratonSuperSecretKey2024!
jwt.expiration=86400000
