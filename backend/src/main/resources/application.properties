# ============================================================
#  Campus Navigation System — Spring Boot Configuration
# ============================================================

spring.application.name=campus-nav

# --- Database ---
spring.datasource.url=jdbc:mysql://mysql-4cf878c-chikamaidavid98-a19f.k.aivencloud.com:10924/defaultdb?useSSL=true&requireSSL=true&serverTimezone=UTC
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_HQh6jQkOoyBY04ij09I
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- SQL Initialization ---
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# --- Server ---
server.port=8080

# --- CORS (allow frontend during dev) ---
cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500

# --- JWT Secret (change in production) ---
jwt.secret=CampusNavBaratonSuperSecretKey2024!
jwt.expiration=86400000
